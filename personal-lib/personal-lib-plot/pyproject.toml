[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "personal-lib-plot"
version = "1"
dependencies = [
	# personal-lib-ann-logic or pred-logic
	# personal-lib-core
	# torch
	# detectron
	"opencv-python",
	"numpy",
]